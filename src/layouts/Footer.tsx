export default function Footer() {
  return (
    <footer className="px-6 py-8 text-center text-xs text-muted">
      A fresh workout, on demand. © {new Date().getFullYear()} repfresh.
    </footer>
  );
}
